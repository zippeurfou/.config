# tmux sessionizer session-name aliases (OPTIONAL).
# By default a session is named after its folder. Add a line here only to OVERRIDE
# a specific folder's session name:
#   <exact ~/Projects folder name>=<custom session name>
# Lines starting with # are ignored.

# homepage_merchant_ranker_gdp=merch-ranker
